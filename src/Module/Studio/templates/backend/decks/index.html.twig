{% extends '@Core/backend/layout.html.twig' %}

{% block title %}{{ 'backend.nav.studio_decks'|trans }} - {{ parent() }}{% endblock %}

{% block page_header_slot %}
    {{ include('@Shared/components/page_header.html.twig', {
        crumbs: [
            {label: 'backend.nav.sections.studio'|trans},
            {label: 'backend.nav.studio_decks'|trans},
        ],
    }) }}
{% endblock %}

{% block body %}
<div {{ vue_component('studio/backend/decks/DecksApp', {
    decks: decks,
    categories: categories,
    customers: customers,
    layouts: layouts,
    showPath: showPath,
    createPath: createPath,
    importPath: importPath,
    updatePath: updatePath,
    deletePath: deletePath,
    duplicatePath: duplicatePath,
    categoryCreatePath: categoryCreatePath,
    categoryUpdatePath: categoryUpdatePath,
    categoryDeletePath: categoryDeletePath,
}) }} class="flex-1 min-w-0"></div>
{% endblock %}
