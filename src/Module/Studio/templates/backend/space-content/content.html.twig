{% extends '@Studio/backend/space-content/layout.html.twig' %}

{% set activeTab = 'content' %}

{% block title %}{{ space.name }}{% endblock %}

{% block body %}
{# Maillon de la colonne : la hauteur descend du corps jusqu'à l'écran
   ouvert, et une div sans classe l'aurait coupée ici. #}
<div class="flex flex-1 flex-col" {{ vue_component('studio/backend/content/SpaceContentApp', {
    space: space,
    columns: columns,
    items: items,
    comments: comments,
    attachments: attachments,
    itemCreatePath: itemCreatePath,
    itemUpdatePath: itemUpdatePath,
    itemDeletePath: itemDeletePath,
    itemReorderPath: itemReorderPath,
    schedulePath: schedulePath,
    commentPostPath: commentPostPath,
    commentDeletePath: commentDeletePath,
    attachmentUploadPath: attachmentUploadPath,
    attachmentAttachPath: attachmentAttachPath,
    attachmentDetachPath: attachmentDetachPath,
    columnCreatePath: columnCreatePath,
    columnUpdatePath: columnUpdatePath,
    columnDeletePath: columnDeletePath,
    columnReorderPath: columnReorderPath,
    chatMessages: chatMessages,
    chatStreamUrl: chatStreamUrl,
    chatPostPath: chatPostPath,
    chatReloadPath: chatReloadPath,
    chatDeletePath: chatDeletePath,
    chatChannels: chatChannels,
    chatChannelId: chatChannelId,
    chatTeam: chatTeam,
    chatChannelCreatePath: chatChannelCreatePath,
    chatChannelRenamePath: chatChannelRenamePath,
    chatChannelAudiencePath: chatChannelAudiencePath,
    chatChannelDeletePath: chatChannelDeletePath,
    chatChannelInvitePath: chatChannelInvitePath,
    chatChannelUninvitePath: chatChannelUninvitePath,
    chatDirectPath: chatDirectPath,
    chatOlderPath: chatOlderPath,
    chatHidePath: chatHidePath,
    chatPeople: chatPeople,
    notes: notes,
    noteCreatePath: noteCreatePath,
    noteUpdatePath: noteUpdatePath,
    noteDeletePath: noteDeletePath,
    notePinPath: notePinPath,
    noteImagePath: noteImagePath,
    craftEnabled: craftEnabled,
    craftDocumentsPath: craftDocumentsPath,
    craftImportPath: craftImportPath,
    craftRefreshPath: craftRefreshPath,
    spaceFiles: spaceFiles,
    spaceFileUploadPath: spaceFileUploadPath,
    spaceFileAttachPath: spaceFileAttachPath,
    spaceFileRemovePath: spaceFileRemovePath,
    driveEnabled: driveEnabled,
    driveFolderId: driveFolderId,
    driveListPath: driveListPath,
    driveFilePath: driveFilePath,
    driveArchivePath: driveArchivePath,
    driveImportPath: driveImportPath,
    driveUnlockPath: driveUnlockPath,
    driveLocked: driveLocked,
    canConfigure: canConfigure,
    settingsPath: settingsPath,
}) }}></div>
{% endblock %}
