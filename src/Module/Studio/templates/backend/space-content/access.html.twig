{% extends '@Studio/backend/space-content/layout.html.twig' %}

{% set activeTab = 'access' %}

{% block title %}{{ space.name }}{% endblock %}

{% block body %}
<div {{ vue_component('studio/backend/access/SpaceAccessApp', {
    space: space,
    links: links,
    defaultValidDays: defaultValidDays,
    maxValidDays: maxValidDays,
    issuePath: issuePath,
    revokePath: revokePath,
    deletePath: deletePath,
    driveFolderId: driveFolderId,
}) }}></div>
{% endblock %}
