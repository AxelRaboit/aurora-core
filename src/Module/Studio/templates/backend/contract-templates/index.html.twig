{% extends '@Core/backend/layout.html.twig' %}

{% block title %}{{ 'backend.nav.studio_contract_templates'|trans }} - {{ parent() }}{% endblock %}

{% block page_header_slot %}
    {{ include('@Shared/components/page_header.html.twig', {
        crumbs: [
            {label: 'backend.nav.sections.studio'|trans},
            {label: 'backend.nav.studio_contract_templates'|trans},
        ],
    }) }}
{% endblock %}

{% block body %}
<div {{ vue_component('studio/backend/contract-templates/ContractTemplatesApp', {
    templates: templates,
    kinds: kinds,
    categories: categories,
    createPath: createPath,
    updatePath: updatePath,
    archivePath: archivePath,
    restorePath: restorePath,
    deletePath: deletePath,
    openDraftPath: openDraftPath,
    duplicatePath: duplicatePath,
    discardDraftPath: discardDraftPath,
    editorPath: editorPath,
}) }} class="flex-1 min-w-0"></div>
{% endblock %}
