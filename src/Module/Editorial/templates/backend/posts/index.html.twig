{% extends '@Core/backend/layout.html.twig' %}

{% block title %}{{ 'backend.nav.posts'|trans }} - {{ parent() }}{% endblock %}

{% block page_header_slot %}
    {{ include('@Shared/components/page_header.html.twig', {
        crumbs: [
            {label: 'backend.nav.sections.editorial'|trans},
            {label: 'backend.nav.posts'|trans},
        ],
    }) }}
{% endblock %}

{% block body %}
<div {{ vue_component('editorial/backend/posts/PostsApp', {
    posts: posts,
    search: search,
    postTypes: postTypes,
    taxonomies: taxonomies,
    locales: locales,
    statusOptions: statusOptions,
    postTypeIds: postTypeIds,
    termIds: termIds,
    statuses: statuses,
    listPath: path('backend_editorial_posts'),
    newPath: path('backend_editorial_posts_new'),
    editPathTemplate: path_template('backend_editorial_posts_edit', {id: '__id__'}),
    deletePathTemplate: path_template('backend_editorial_posts_delete', {id: '__id__'}),
    duplicatePathTemplate: path_template('backend_editorial_posts_duplicate', {id: '__id__'}),
    previewPathTemplate: path_template('backend_editorial_posts_preview', {id: '__id__'}),
    bulkPath: path('backend_editorial_posts_bulk'),
}) }} class="flex-1 min-w-0"></div>
{% endblock %}
